name: Deploy reviewed locale pages

on:
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"
      - name: Enforce human review status
        run: node scripts/i18n_check.mjs --release
      - name: Build static locale pages
        run: node scripts/i18n_build.mjs
      - name: Validate generated site
        run: python3 scripts/site_smoke.py
      - name: Validate CMS-driven customizer contract
        run: node scripts/customizer_cms_contract_check.mjs
      - name: Validate CMS site-content runtime contract
        run: node scripts/site_content_runtime_check.mjs
      - name: Validate support handoff contract
        run: node scripts/support_handoff_check.mjs
      - name: Validate visitor order contract
        run: node scripts/order_frontend_contract_check.mjs
      - name: Validate standalone repository delivery
        run: node scripts/verify_repository_delivery.mjs
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: .

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
